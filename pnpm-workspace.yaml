packages:
  - "packages/*"
  - "apps/*"
allowBuilds:
  # esbuild ships prebuilt binaries and needs its install script to select one.
  # Required by tsx, which runs the eval harness directly from TypeScript.
  esbuild: true
